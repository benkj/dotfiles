 -- Use buffer source for `/` and `?` (if you enabled `native_menu`, this won't work anymore).
 require('lualine').setup {
     options = {
         icons_enabled = true,
         --theme = 'base16',
         theme = 'base16',
     },
 }

